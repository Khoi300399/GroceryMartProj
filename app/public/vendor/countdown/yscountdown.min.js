(function () {
  "use strict";

  /**
   * Countdown timer
   */
  var $cd = $("#cd");
  if ($cd.length > 0) {
    let cd = document.getElementById("cd");
    const output = cd.innerHTML;

    const countDownDate = function () {
      let timeleft =
        new Date(cd.getAttribute("data-countdown")).getTime() -
        new Date().getTime();

      let days = Math.floor(timeleft / (1000 * 60 * 60 * 24));
      let hours = Math.floor(
        (timeleft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)
      );
      let minutes = Math.floor((timeleft % (1000 * 60 * 60)) / (1000 * 60));
      let seconds = Math.floor((timeleft % (1000 * 60)) / 1000);

      cd.innerHTML = output
        .replace("%d", days)
        .replace("%h", hours)
        .replace("%m", minutes)
        .replace("%s", seconds);
    };
    countDownDate();
    setInterval(countDownDate, 1000);
  }
  /**
   * Countdown timer
   */
  var $cd1 = $("#cd1");
  if ($cd1.length > 0) {
    let cd1 = document.getElementById("cd1");
    const output = cd1.innerHTML;

    const countDownDate = function () {
      let timeleft =
        new Date(cd1.getAttribute("data-countdown")).getTime() -
        new Date().getTime();

      let days = Math.floor(timeleft / (1000 * 60 * 60 * 24));
      let hours = Math.floor(
        (timeleft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)
      );
      let minutes = Math.floor((timeleft % (1000 * 60 * 60)) / (1000 * 60));
      let seconds = Math.floor((timeleft % (1000 * 60)) / 1000);

      cd1.innerHTML = output
        .replace("%d", days)
        .replace("%h", hours)
        .replace("%m", minutes)
        .replace("%s", seconds);
    };
    countDownDate();
    setInterval(countDownDate, 1000);
  }

  /**
   * Countdown timer
   */
  var $cd2 = $("#cd2");
  if ($cd2.length > 0) {
    let cd2 = document.getElementById("cd2");
    const output = cd2.innerHTML;

    const countDownDate = function () {
      let timeleft =
        new Date(cd2.getAttribute("data-countdown")).getTime() -
        new Date().getTime();

      let days = Math.floor(timeleft / (1000 * 60 * 60 * 24));
      let hours = Math.floor(
        (timeleft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)
      );
      let minutes = Math.floor((timeleft % (1000 * 60 * 60)) / (1000 * 60));
      let seconds = Math.floor((timeleft % (1000 * 60)) / 1000);

      cd2.innerHTML = output
        .replace("%d", days)
        .replace("%h", hours)
        .replace("%m", minutes)
        .replace("%s", seconds);
    };
    countDownDate();
    setInterval(countDownDate, 1000);
  }
})();
1;
